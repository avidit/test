name: pwsh

on: [push, workflow_dispatch]

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: error
        shell: pwsh
        run: |
          $files = Get-ChildItem -Path ${{ github.workspace }}\.github -Recurse
          $title = "Missing version information"
          $message = $files | ForEach-Object { $_.FullName }
          $message2 = foreach ($file in $files) {$file.FullName}
          Write-Output "::error title=one::$message"
          Write-Output "::error title=two::$message1"