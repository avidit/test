name: pwsh

on: [push, workflow_dispatch]

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: one
        shell: pwsh
        run: Write-Output step one
      - name: two
        shell: pwsh
        run: Write-Error step two
      - name: three
        shell: pwsh
        run: Write-Host step three
