name: Move new issues to Triage

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.8.1
        with:
          project: DynamoWishlist
          column: Triage
          repo-token: ${{ secrets.DYNAMO_WISHLIST_TOKEN }}
